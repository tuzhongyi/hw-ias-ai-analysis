export enum SyatemMainMapNavigation {
  main = 'main',
  shop = 'shop',
  realtime = 'realtime',
  gpstask = 'gpstask',
  heatmap = 'heatmap',
  roadobject = 'roadobject',
}
export enum EventMode {
  shop,
  realtime,
  gpstask,
  roadobject,
}
