export enum SyatemMainMapNavigation {
  main = 'main',
  shop = 'shop',
  realtime = 'realtime',
  gpstask = 'gpstask',
  heatmap = 'heatmap',
}
export enum EventMode {
  shop,
  realtime,
  gpstask,
}
